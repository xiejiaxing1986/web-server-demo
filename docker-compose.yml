services:
  database:
    image: postgres:15
    restart: always
    env_file:
      - .env
    volumes:
      - data:/var/lib/postgresql/data
  server:
    build: .
    restart: always
    env_file:
      - .env
    depends_on:
      - database
    ports:
    - "8080:8080"
volumes:
  data: