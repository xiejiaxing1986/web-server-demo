services:
  database:
    image: postgres:15
    restart: always
    env_file:
    - .env
    volumes:
    - pg-data:/var/lib/postgresql/data
  server:
    build: .
    restart: always
    env_file:
    - .env
    depends_on:
    - database
    ports:
    - "8080:8080"
volumes:
  pg-data: